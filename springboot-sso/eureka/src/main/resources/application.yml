spring:
  application:
    name: eurekaserver
  profiles:
    active: dev

server:
  port: 7001

eureka:
  instance:
    hostname: eureka1
  client:
    register-with-eureka: false
    fetch-registry: false

